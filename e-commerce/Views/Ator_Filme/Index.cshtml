@model IEnumerable<e_commerce.Models.Ator_Filme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ator.AtorFotoLink)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ator.AtorFotoLink)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FilmeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FilmeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FilmeId })
        </td>
    </tr>
}

</table>
